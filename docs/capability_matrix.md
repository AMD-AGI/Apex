# Apex capability matrix

This matrix separates implemented contracts from live qualification. A CPU or
synthetic test proves schema and control-flow behavior; it is not evidence that a
GPU workload, model backend, or external evaluator passed. Live campaign receipts
replace the `pending` cells only after the named run completes.

| Surface | Implemented contract | Current qualification | Deliberate boundary |
|---|---|---|---|
| General coding-agent CLI | Backend-native Codex/Claude/Cursor interactive, headless, and resume launch; integrity-checked AMD optimization/debugging/HIP-method skills; a descriptor-free unverified kernel draft plus host-owned `--campaign` continuation; bounded credential-redacted backend doctor; read-only digest-bound GPU ownership/process-context/ROCm-health doctor | CPU argv, skill-package, draft-only MCP grant, exact-digest handoff, journal, redaction, ownership/cgroup/Slurm/NHC/ROCm-health doctor, and fake-lease tests pass; one Codex/Triton live pilot reached an honest `no_gain` terminal, while the full backend matrix remains pending | The HIP skill is instruction-only and does not enable generic HIP execution; Cursor has no MCP bridge; live chat cannot invoke evaluator or acquisition mutations; exact user confirmation after chat exit is required before the existing formal optimizer may acquire a GPU |
| Natural-language kernel CLI | Resolves prose through a trusted task descriptor; unresolved input returns atomic `needs_input` | CPU unit/contract tests | Prose cannot expand editable files or invent evaluator commands |
| Machine kernel CLI | Caller-neutral `TaskSpec`, isolated fresh attempts, default Codex with Claude/Cursor overrides | CPU multi-attempt/backend contracts plus one real MI355X Triton campaign with compile, correctness, measurement, grade, and terminal `no_gain` | No legacy CLI/state/result reader |
| Kernel correctness | Fixed-argv compile and correctness checks bound to frozen source bytes; phased MCP tools reconstruct the CAS candidate under an evaluator-owned projection | CPU adversarial/chat-recovery tests and the MI355X Triton pilot compile/correctness gates passed | Agent stdout and self-reported speedup are never verdicts |
| Kernel performance | Invocation-level raw samples, seeded ABBA blocks, GPU-health brackets, median/p99 robust grade; MCP capture and grade are separate reward boundaries | Statistical contracts pass; the MI355X pilot produced 300 raw samples per implementation and evaluator reward 320, but excessive candidate timing variance forced REVERT and terminal task reward 120 | Missing/invalid p99 has null reward; aggregate/batched means cannot impersonate p99; an attempt reward is not a promoted task result |
| Safety | Typed capability/execution/finding, immutable plan and candidate evidence, required/advisory policy | CPU truth-table and isolation tests | Unsupported or inconclusive never means clean; AKA remains its own authority |
| Kernel delivery | Content-digested source patch bundle; evaluator-owned `bundle.build`; scoped read-only `bundle.verify`; explicit exact-clean-baseline apply with rollback | CPU integrity/apply/MCP authority and formal-delivery tests | Build requires a verified improving grade; default optimization never mutates the caller repository; verification never grades |
| E2E intake | Exact Magpie config, Docker one-shot V2 boundary, kernel-only scope, frozen throughput/accuracy/TTFT/TPOT gates | CPU config-invariant and early scope-rejection tests | Local, Ray, reuse, and cleanup return `e2e_docker_only` before provenance/GPU/agent; config-only winners are forbidden |
| E2E exact-image quality evaluator | Production-composed Docker lm-eval sidecar with a locked offline dataset, run-scoped reverified input projection, private InferenceX task/launch projection, root-container listener probe, Unix handoff and single-listener broker, no network/GPU, read-only root, exact image/runtime identity, bounded output publication, and lifecycle/cleanup receipts | CPU authority/tamper tests plus real exact-image root-listener and sidecar `create/inspect/remove` smokes pass; Qwen baseline performance is live, while full lm-eval model-quality qualification remains pending | Composition, lifecycle smoke, and throughput do not prove model quality, reward eligibility, a qualified Magpie workflow, or release readiness |
| E2E control plane | Typed state machine, fresh bounded contexts, current-anchor KEEP/REVERT, replayable journal, scoped read-only `campaign.status`, journal-preserving `campaign.checkpoint`, and `campaign.resume` delegation to the formal use case | CPU transition/resume/MCP status/checkpoint/delegation tests; live crash/resume pending | Conversation history and disposable snapshots are not canonical state; resume revalidates release/dependency/provenance/GPU identity and never invents a second recovery loop |
| E2E diagnostics | Magpie owns acquisition, TraceLens owns analysis/comparison, Apex independently validates semantic accounting, enforces kernel-family planning coverage, ranks opportunities, and exposes reward-ineligible scoped tools | CPU normalization/ranking/comparison/forged-summary contracts and pinned TraceLens API smoke pass; current live qualification pending. The older Qwen v19 partial trace is retained only as a [historical diagnostic record](validation_qwen3_next_80b_fp8.md) and fails the current coverage gate | Apex has no second low-level tracing engine; comparison is not a grade and full attribution remains unavailable |
| E2E source delivery | Exact source locks, cumulative patches, immutable derived image, loaded-byte/build evidence, second clean replay | CPU build/replay adversarial tests; live Qwen run ended `no_gain`, so formal winner replay remains unqualified | Overlay success is only `runtime_overlay_verified`; formal success requires rebuild and clean replay |
| Supported source changes | Standalone Python/Triton; exact packaged template-bound image kernels; E2E exact-lock source-build contract | Template materialization/source-authority CPU tests; all three template campaigns pending | Caller-authored HIP fails with `hip_execution_unavailable`; no generic HIP, monolithic extension, or system ROCm-library support |
| Knowledge | Attributed GEAK raw snapshot, normalized advisory cards, deterministic scoped retrieval, event-derived experience view | Corpus validation and retrieval tests | Knowledge cannot alter trusted policy, correctness, or measured reward |
| RL environment | Canonical event/CAS trajectory, CAS-backed typed formal MCP calls/results, parent/attempt episode graph, deterministic RL/SFT export with split/visibility | CPU replay/export and chat-to-formal tool-pair tests | Read-only status does not mutate the trajectory; missing artifacts and held-out-private leakage fail closed |
| Dependencies | One-command exact-pin Magpie/TraceLens/InferenceX install and receipt verification; Apex projects published Magpie main configs into content-bound scoring/capability contracts; a GPU-free production-preview collector exercises the actual E2E composition for the frozen corpus | Magpie's public loader resolves all 27 configs at `main@12896a49`; the V2 production preview accepts all 21 Docker one-shot rows with benchmark/formal-measurement composition available and rejects the 6 Local/Ray/reuse/cleanup rows as `e2e_docker_only`; among the accepted rows, 17 source routes are `evidence_pending` and 4 require a source adapter; live qualification remains pending | Runtime auto-clone of moving branches, unpublished Magpie APIs, and treating either the 27-row ledger or non-authoritative preview manifest as live evidence are forbidden |
| Release candidate | One rebuilt receipt exposes a non-circular baseline gate (clean reviewed source + dependencies + all-corpus Apex Magpie config-resolution evidence + CPU + CLI) for starting live work and a stricter final gate adding images, seven live qualifications, and four verified showcases | CPU/tamper contracts pass; current baseline/release states remain blocked by the dirty development tree and missing live evidence | A self-hash, old test log, config-compatible ledger, pending template, screenshot, or positive control cannot authorize live work/release |
| AgentKernelArena | Three attributed task-input snapshots with exact upstream commit/tree/file/license receipts; AKA launcher/scoring remains external | Input registry/import checks pass; Apex evaluators, immutable v0.24.0 image/source pins, showcases, and matched GPU campaign pending | Apex imports no AKA runner, Forge adapter, scorer, validator, hidden checks, or historical result |

The clean-cut deletion ledger is
[`deletion_inventory.yaml`](../deletion_inventory.yaml). It assigns every removed
production, test, documentation, config, and data file to a replacement owner and
is enforced by the architecture zero-reference gate.
