apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: keystone

resources:
  - ../namespace.yaml
  - pvc.yaml
  - mcp-deployment.yaml
  - eval-job.yaml

commonLabels:
  app.kubernetes.io/part-of: keystone
